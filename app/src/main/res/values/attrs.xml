<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Orientation of a widget. -->
    <attr name="direction">

        <!-- Vertical widget. -->
        <enum name="vertical" value="0" />
        <!-- Horizontal widget. -->
        <enum name="horizontal" value="1" />
    </attr>

    <skip />

    <!--
         Workspace specific attributes. These attributes are used to customize
         the workspace in XML files.
    -->
    <declare-styleable name="Workspace">

        <!-- The first screen the workspace should display. -->
        <attr name="defaultScreen" format="integer" />
    </declare-styleable>

    <!--
         CellLayout specific attributes. These attributes are used to customize
         a CellLayout view in XML files.
    -->
    <declare-styleable name="CellLayout">

        <!-- The width of a single cell -->
        <attr name="cellWidth" format="dimension" />
        <!-- The height of a single cell -->
        <attr name="cellHeight" format="dimension" />
        <!-- Padding to apply at the long axis -->
        <attr name="longAxisPadding" format="dimension" />
        <!-- Padding to apply at the short axis -->
        <attr name="shortAxisPadding" format="dimension" />
        <!-- Number of cells on the short axis of the CellLayout -->
        <attr name="shortAxisCells" format="integer" />
        <!-- Number of cells on the long axis of the CellLayout -->
        <attr name="longAxisCells" format="integer" />
    </declare-styleable>

    <!--
         DeleteZone specific attributes. These attributes are used to customize
         a DeleteZone view in XML files.
    -->
    <declare-styleable name="DeleteZone">

        <!-- Orientation of the delete zone. -->
        <attr name="direction" />
    </declare-styleable>

    <!--
         HandleView specific attributes. These attributes are used to customize
         a HandleView view in XML files.
    -->
    <declare-styleable name="HandleView">

        <!-- Orientation of the handle. -->
        <attr name="direction" />
    </declare-styleable>

    <!-- XML attributes used by default_workspace.xml -->

    <declare-styleable name="Favorite">
        <attr name="shortid" format="integer" />
        <attr name="title" format="string" />
        <attr name="url" format="string" />
        <attr name="container" format="string" />
        <attr name="screen" format="string" />
        <attr name="itemindex" format="string" />
        <attr name="itemtype" format="string" />
        <attr name="icontype" format="string" />
        <attr name="icon" format="reference" />
        <attr name="iconresource" format="string" />
    </declare-styleable>
    <declare-styleable name="ClippedImageView">
        <attr name="ignoreZone" format="dimension" />
    </declare-styleable>
   

    <!-- LockPatternView class attributes -->
    <declare-styleable name="LockPatternView">

        <!--
             Aspect to use when drawing LockPatternView. Choices are "square"(default), "lock_width"
             or "lock_height"
        -->
        <attr name="aspect">
            <enum name="square" value="0" />
            <enum name="lockWidth" value="1" />
            <enum name="lockHeight" value="2" />
        </attr>
        <attr name="line" format="color" />
        <attr name="btnNormal" format="reference" />
        <attr name="btnTouched" format="reference" />
        <attr name="circleNormal" format="reference" />
        <attr name="circleGreen" format="reference" />
        <attr name="circleRed" format="reference" />
        <attr name="arrowGreenUp" format="reference" />
        <attr name="arrowRedUp" format="reference" />
    </declare-styleable>
    <declare-styleable name="ViewfinderView"></declare-styleable>
    <declare-styleable name="Gallery">
        <attr name="android:galleryItemBackground" />
    </declare-styleable>

    
    <declare-styleable name="RoundImageView">
        <attr name="border_inside_color" format="color" /> <!-- 画内圆颜色   -->
         <attr name="border_outside_color" format="color" /> <!-- 画外圆颜色  -->
        <attr name="border_thickness" format="dimension" />
    </declare-styleable>
    
    <declare-styleable name="View">
        <attr name="fadingEdgeLength" format="dimension" />
    </declare-styleable>

    
     <declare-styleable name="DragSortListView">
        <attr name="collapsed_height" format="dimension" />
        <attr name="drag_scroll_start" format="float" />
        <attr name="max_drag_scroll_speed" format="float" />
        <attr name="float_background_color" format="color" />
        <attr name="remove_mode">
            <enum name="clickRemove" value="0" />
            <enum name="flingRemove" value="1" />
        </attr>
        <attr name="track_drag_sort" format="boolean" />
        <attr name="float_alpha" format="float" />
        <attr name="slide_shuffle_speed" format="float" />
        <attr name="remove_animation_duration" format="integer" />
        <attr name="drop_animation_duration" format="integer" />
        <attr name="drag_enabled" format="boolean" />
        <attr name="sort_enabled" format="boolean" />
        <attr name="remove_enabled" format="boolean" />
        <attr name="drag_start_mode">
            <enum name="onDown" value="0" />
            <enum name="onMove" value="1" />
            <enum name="onLongPress" value="2" />
        </attr>
        <attr name="drag_handle_id" format="integer" />
        <attr name="fling_handle_id" format="integer" />
        <attr name="click_remove_id" format="integer" />
        <attr name="use_default_controller" format="boolean" />
    </declare-styleable>
    
     <declare-styleable name="PullToRefresh">
		<attr name="adapterViewBackground" format="reference|color" />
		<attr name="headerBackground" format="reference|color" />
		<attr name="headerTextColor" format="color" />
		<attr name="mode">
			<flag name="pullDownFromTop" value="0x1" />
			<flag name="pullUpFromBottom" value="0x2" />
			<flag name="both" value="0x3" />
		</attr>
	</declare-styleable>
</resources>